using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class revisao : MonoBehaviour
{
    int = nInteriro = 1;
    float = nQuebrado = 1.2f;
    string = nTexto = "Lari";
    // Start is called before the first frame update
    void Start()
    {
        print("eu sou legal");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
